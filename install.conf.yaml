- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.config/neofetch: neofetch

- shell:
  - [git submodule update --init --recursive, Installing submodules]
