- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: vim
    ~/.config/neofetch: neofetch
    #~/.zshrc: zshrc
  - shell:
  - [git submodule update --init --recursive, Installing submodules]
